spring:
  cloud:
    config:
      uri: http://localhost:8580   # 配置中心服务的地址
      name: application-test   # 要读取的配置文件application属性
      #label: test
      #profile: dev # 要读取的配置文件profile属性
      #label: master   # git 配置文件所在分支
